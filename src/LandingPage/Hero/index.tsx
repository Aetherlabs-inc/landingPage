'use client'
import React from 'react';

// This component is no longer used - replaced by Story component
// Keeping it for backward compatibility but it won't render
const HeroSection = () => {
    return null;
};

export default HeroSection;